---
name: E2E Tests

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      test-suite:
        description: "Test suite (vmaas, caas, or empty for all)"
        required: false
        default: "vmaas"
      test-filter:
        description: "pytest -k filter"
        required: false
        default: ""

jobs:
  e2e-vmaas:
    uses: ./.github/workflows/e2e-vault-secrets.yml
    with:
      test-suite: ${{ github.event.inputs.test-suite || 'vmaas' }}
      test-filter: ${{ github.event.inputs.test-filter || '' }}
